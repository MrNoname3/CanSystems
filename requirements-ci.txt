# CI-only tooling, pinned exact so a release of either cannot change what the gate runs without
# a commit. Not installed locally: PlatformIO runs from its own penv there (see AGENTS.md), and
# intelhex already lives in that penv.
#
#   pioarduino - the gate's build/test/check driver: the PlatformIO core fork the pioarduino IDE
#                installs locally, so CI runs the same core.
#   intelhex   - imported by the ESP32 esptool (tool-esptoolpy) when it creates the flash image;
#                pioarduino requires it unpinned.
pioarduino==6.2.0
intelhex==2.3.0
