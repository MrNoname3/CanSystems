# CLAUDE.md

The instructions for coding agents on this repository live in [AGENTS.md](AGENTS.md) — every other
agent looks for that name, so it holds the only copy. The line at the bottom is what hands it to
Claude Code as well: the file is attached as an instruction block of its own, so nothing has to be
read for it.

Nothing belongs in this file besides that. Two files saying the same thing is how they stop saying
the same thing — and if the import below ever stops working, this sentence is what tells the
reader where to go instead.

@AGENTS.md
